<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mhf.dao.CartMapper">
    <resultMap id="BaseResultMap" type="com.mhf.pojo.Cart">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="product_id" jdbcType="INTEGER" property="productId"/>
        <result column="quantity" jdbcType="INTEGER" property="quantity"/>
        <result column="checked" jdbcType="INTEGER" property="checked"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        delete from cart
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <insert id="insert" parameterType="com.mhf.pojo.Cart">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        insert into cart (id, user_id, product_id,
        quantity, checked, create_time,
        update_time)
        values (#{id,jdbcType=INTEGER}, #{userId,jdbcType=INTEGER}, #{productId,jdbcType=INTEGER},
        #{quantity,jdbcType=INTEGER}, #{checked,jdbcType=INTEGER}, now(),
        now())
    </insert>
    <update id="updateByPrimaryKey" parameterType="com.mhf.pojo.Cart">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        update cart
        set
        product_id = #{productId,jdbcType=INTEGER},
        quantity = #{quantity,jdbcType=INTEGER},
        checked = #{checked,jdbcType=INTEGER},
        update_time = now()
        where id = #{id,jdbcType=INTEGER}
    </update>
    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        select id, user_id, product_id, quantity, checked, create_time, update_time
        from cart
        where id = #{id,jdbcType=INTEGER}
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        select id, user_id, product_id, quantity, checked, create_time, update_time
        from cart
    </select>

    <select id="selectCartByUserIdAndProductId" parameterType="map" resultMap="BaseResultMap">
        <!--
          根据userId和商品productId查询购物信息
        -->
        select id, user_id, product_id, quantity, checked, create_time, update_time
        from cart
        where user_id = #{userId} and product_id = #{productId}
    </select>

    <select id="selectCartByUserId" parameterType="int" resultMap="BaseResultMap">
        <!--
          根据userId查询购物信息
        -->
        select id, user_id, product_id, quantity, checked, create_time, update_time
        from cart
        where user_id = #{userId}
    </select>

    <select id="isCheckedAll" parameterType="int" resultType="int">
        <!--
          统计用户购物信息是否全选(统计未选中商品)
        -->
        select count(1)
        from cart
        where user_id = #{userId} and checked = 0
    </select>

    <delete id="deleteByUserIdAndProductId" parameterType="map">
        <!--
          删除购物车商品
        -->
        delete from cart
        <where>
            user_id = #{userId}
            <if test="productIds != null and productIds.size > 0">
                and product_id in
                <foreach collection="productIds" item="productId" open="(" close=")" separator=",">
                    #{productId}
                </foreach>
            </if>
        </where>
    </delete>

    <update id="selectOrUnselectProduct" parameterType="map">
        <!--
         购物车选中、反选（更新checked字段）
        -->
        update cart
        set
        checked = #{check}
        <where>
            user_id = #{userId}
            <if test="productId != null">
                and product_id = #{productId}
            </if>
        </where>
    </update>

    <select id="getCartProductCount" parameterType="int" resultType="int">
        <!--
          购物车商品数量
        -->
        select ifnull(sum(quantity),0)
        from cart
        where user_id = #{userId}
    </select>

    <select id="findCartListByUserIdAndChecked" parameterType="map" resultMap="BaseResultMap">
        <!--
          查询已选中商品
        -->
        select id, user_id, product_id, quantity, checked, create_time, update_time
        from cart
        where user_id = #{userId,jdbcType=INTEGER} and checked = #{checked};
    </select>

    <!-- 批量删除购物车商品 -->
    <delete id="batchDelete" parameterType="list">
        delete from cart
        <where>
            id in
            <foreach collection="list" item="cart" open="(" close=")" separator=",">
                #{cart.id}
            </foreach>
        </where>
    </delete>


</mapper>