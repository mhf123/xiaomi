<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mhf.dao.ProductMapper">
    <resultMap id="BaseResultMap" type="com.mhf.pojo.Product">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="category_id" jdbcType="INTEGER" property="categoryId"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="subtitle" jdbcType="VARCHAR" property="subtitle"/>
        <result column="color1" jdbcType="VARCHAR" property="color1"/>
        <result column="color2" jdbcType="VARCHAR" property="color2"/>
        <result column="main_image" jdbcType="VARCHAR" property="mainImage"/>
        <result column="price" jdbcType="DECIMAL" property="price"/>
        <result column="stock" jdbcType="INTEGER" property="stock"/>
        <result column="status" jdbcType="INTEGER" property="status"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
        <result column="sub_images" jdbcType="LONGVARCHAR" property="subImages"/>
        <result column="detail" jdbcType="LONGVARCHAR" property="detail"/>
    </resultMap>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        delete from product
        where id = #{id,jdbcType=INTEGER}
    </delete>
    <insert id="insert" parameterType="com.mhf.pojo.Product">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        insert into product (category_id, name,
        subtitle, main_image, price,
        stock, status, create_time,
        update_time, sub_images, detail,color1,color2
        )
        values (#{categoryId,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR},
        #{subtitle,jdbcType=VARCHAR}, #{mainImage,jdbcType=VARCHAR}, #{price,jdbcType=DECIMAL},
        #{stock,jdbcType=INTEGER}, #{status,jdbcType=INTEGER},now(),
        now(), #{subImages,jdbcType=LONGVARCHAR}, #{detail,jdbcType=LONGVARCHAR},
        #{color1}, #{color2}
        )
    </insert>
    <update id="updateByPrimaryKey" parameterType="com.mhf.pojo.Product">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        update product
        set category_id = #{categoryId,jdbcType=INTEGER},
        name = #{name,jdbcType=VARCHAR},
        subtitle = #{subtitle,jdbcType=VARCHAR},
        main_image = #{mainImage,jdbcType=VARCHAR},
        price = #{price,jdbcType=DECIMAL},
        stock = #{stock,jdbcType=INTEGER},
        status = #{status,jdbcType=INTEGER},
        update_time = now(),
        sub_images = #{subImages,jdbcType=LONGVARCHAR},
        detail = #{detail,jdbcType=LONGVARCHAR},
        color1 = #{color1},
        color2 = #{color2}
        where id = #{id,jdbcType=INTEGER}
    </update>
    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        select id, category_id, name, subtitle, main_image, price, stock, status, create_time,
        update_time, sub_images, detail ,color1,color2
        from product
        where id = #{id,jdbcType=INTEGER}
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        select id, category_id, name, subtitle, main_image, price, stock, status, create_time,
        update_time, sub_images, detail,color1,color2
        from product
    </select>

    <!-- 更新商品 -->
    <update id="updateProduct" parameterType="com.mhf.pojo.Product">
        update product
        <set>
            <if test="categoryId != null">
                category_id = #{categoryId,jdbcType=INTEGER},
            </if>
            <if test="name != null">
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="subtitle != null">
                subtitle = #{subtitle,jdbcType=VARCHAR},
            </if>
            <if test="mainImage != null">
                main_image = #{mainImage,jdbcType=VARCHAR},
            </if>
            <if test="price != null">
                price = #{price,jdbcType=DECIMAL},
            </if>
            <if test="stock != null">
                stock = #{stock,jdbcType=INTEGER},
            </if>
            <if test="status != null">
                status = #{status,jdbcType=INTEGER},
            </if>
            <if test="subImages != null">
                sub_images = #{subImages,jdbcType=LONGVARCHAR},
            </if>
            <if test="detail != null">
                detail = #{detail,jdbcType=LONGVARCHAR},
            </if>
            <if test="color1 != null">
                color1 = #{color1},
            </if>
            <if test="color2 != null">
                color2 = #{color2},
            </if>
            update_time = now()
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
    <!-- 通过id、name模糊查询商品 -->
    <select id="findProductByIdOrName" parameterType="map" resultMap="BaseResultMap">
        select id, category_id, name, subtitle, main_image, price, stock, status, create_time,
        update_time, sub_images, detail ,color1 ,color2
        from product
        <where>
            <if test="status != null">
                status = #{status}
            </if>
            <if test="productId != null">
                and id = #{productId}
            </if>
            <if test="productName != null">
                and name like #{productName}
            </if>
        </where>
    </select>

    <!-- 搜索商品 -->
    <select id="searchProduct" parameterType="map" resultMap="BaseResultMap">
        select id, category_id, name, subtitle, main_image, price, stock, status, create_time,
        update_time, sub_images, detail ,color1 ,color2
        from product
        <where>
            <if test="integerSet != null and integerSet.size > 0">
                category_id in
                <foreach collection="integerSet" item="category_id" open="(" close=")" separator=",">
                    #{category_id}
                </foreach>
            </if>
            <if test="keyword != null">
                and name like #{keyword}
            </if>
        </where>
        group by name

    </select>
    <select id="selectByNameAndDetailAndColor" parameterType="com.mhf.pojo.Product" resultType="int">
        <!--
          根据商品name、detail、color查询
        -->
        select count(1)
        from product
        where name = #{name} and detail = #{detail} and color2 = #{color2}
    </select>

    <select id="findStockById" parameterType="int" resultType="int">
        <!--
          根据商品id查询商品库存
        -->
        select stock
        from product
        where id = #{id}
        for update
    </select>

    <!-- 根据分类查找商品名称列表 -->
    <select id="nameList" parameterType="map" resultMap="BaseResultMap">
        select  id, category_id, name, subtitle, main_image, price, stock, status, create_time,
        update_time, sub_images, detail ,color1 ,color2
        from product
        <where>
            <if test="integerSet != null and integerSet.size > 0">
                category_id in
                <foreach collection="integerSet" item="category_id" open="(" close=")" separator=",">
                    #{category_id}
                </foreach>
            </if>
        </where>
        group by name

    </select>

    <!-- 按需查询未下架的商品列表 -->
    <select id="selectByDemand" parameterType="map" resultMap="BaseResultMap">
        select id, category_id, name, subtitle, main_image, price, stock, status, create_time,
        update_time, sub_images, detail ,color1 ,color2
        from product
        <where>
            name = #{productName} and status = 1
            <if test="detail != null">
                and detail = #{detail}
            </if>
            <if test="color != null">
                and color2 = #{color}
            </if>
            <if test="detail == null">
                group by detail
            </if>

        </where>

    </select>

    <!-- 按需查询未下架的商品列表 -->
    <select id="selectSuggestListByKeyword" parameterType="string" resultMap="BaseResultMap">
        select name
        from product
        where name like #{keyword}
        group by name
    </select>
</mapper>